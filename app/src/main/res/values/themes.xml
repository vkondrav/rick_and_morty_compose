<resources>
    <!-- Splash Screen Theme. -->
    <style name="Theme.App.Starting" parent="Theme.SplashScreen">
        <item name="windowSplashScreenBackground">#FFFFFF</item>
        <item name="windowSplashScreenAnimatedIcon">@drawable/ic_anim_splash</item>
        <item name="windowSplashScreenAnimationDuration">1000</item>
    </style>

    <!-- Base application theme. -->
    <style name="Theme.DesignSystemCompose" parent="Theme.MaterialComponents.DayNight.DarkActionBar">
        <!-- Status bar color. -->
        <item name="android:statusBarColor">#FF192038</item>
    </style>

    <style name="Theme.DesignSystemCompose.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>

    <style name="Theme.DesignSystemCompose.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />

    <style name="Theme.DesignSystemCompose.PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />
</resources>
